<?php
/*include_once "../data/meta.php";

$articulo = json_decode(file_get_contents("php://input"));

//echo "codigo".$articulo->codigo;
$meta = new meta();
$meta->eliminarArticulo(strtoupper($articulo->codigo));

    public function eliminarArticulo($codigo){
        $query = "BEGIN eliminarArticulo(:codigo); END;";
        $this->conDB->parse($query);
        $this->conDB->vincular(':codigo',$codigo);
        $this->conDB->Ejecutar();
        $this->conDB->cerrarConexion();
    }
    

    $id=$_POST['codigo'];
	$query="CALL eliminarArticulo('$id')";
	$this->conDB->parse($query);
    $this->conDB->vincular(':codigo',$codigo);
    $this->conDB->Ejecutar();
    $this->conDB->cerrarConexion();
	*/
	alert(codigo,'Hola');

?>

